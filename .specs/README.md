# Specifications

Specifications describe scoped, reviewable changes to Tiny Taverns. They are the source of truth for what gets built before any code lands.

## Active specs

- [campaign-scoped-npcs.md](./campaign-scoped-npcs.md) — closes the `CONTEXT.md:72` flagged ambiguity by making every NPC belong to exactly one Campaign, including the database schema, the RPC shape, the AI toolkit context, and the client route structure.

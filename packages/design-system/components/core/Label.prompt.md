Standalone 13px medium label. Required companion to `Input` and `Select`, which render no label of their own (matching shadcn).

```jsx
<div style={{ display: "grid", gap: 6 }}>
  <Label htmlFor="enc">Encounter name</Label>
  <Input id="enc" placeholder="Goblin ambush" />
</div>
```

---
"@app/server": minor
---

Add `NpcRepo` integration tests covering the new campaign and user scoping in the SQL `WHERE` clauses, and cursor pagination. Mirrors the existing `ChatRepo` integration test style.
